# Test repository with 2k branches

To create test branches, run:

```sh
node create-branches.mjs
```

Warning: this may take a while.

To push all branches to the origin, use:

```sh
git push origin --all
```
